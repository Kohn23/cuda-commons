# Common Utils for CUDA Programming

This repo is intended to be used as a git submodule for convenience(maybe). It is not a pure header-only library unfortunately.

> **To use this:** you must add the root directory to your include path.

## Layout